#include <stdio.h>
#include <math.h>

int main(){

    int num=0, result=0;

        printf("Digite um numero: ");
        scanf("%d",&num);

        result = pow(num,2);
        printf("%d",result);
    return 0;
}